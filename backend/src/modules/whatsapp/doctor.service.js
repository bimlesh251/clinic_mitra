// import {findByClinic} from "../doctor/doctor.repository.js"
// export const getDoctors = async (clinicId) => {

//     const doctors = await findByClinic(clinicId);

//     return doctors.map(doc => ({

//         id: doc._id.toString(),
        
//         title: doc.name,
//         description: doc.specialization

//     }));

// };

// export const getDoctor = (doctorId) => {

//     return doctors.find(d => d.id === doctorId);

// };